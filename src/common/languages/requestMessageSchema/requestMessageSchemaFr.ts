import { TRequestSchemaMessage } from '../../types'

export const requestMessageSchemaFr: TRequestSchemaMessage = {
  data: {
    isObject: 'Le champ de données doit être un objet',
    isRequired: 'Le champ de données est requis'
  }
}
