import { TRequestSchemaMessage } from '../../types'

export const requestMessageSchemaEn: TRequestSchemaMessage = {
  data: {
    isObject: 'The data field must be an object',
    isRequired: 'The data field is required'
  }
}
