import { TRequestSchemaMessage } from '../../types'

export const requestMessageSchemaPt: TRequestSchemaMessage = {
  data: {
    isObject: 'O campo data deve ser um objeto',
    isRequired: 'O campo data é obrigatório'
  }
}
