import { TRequestSchemaMessage } from '../../types'

export const requestMessageSchemaEs: TRequestSchemaMessage = {
  data: {
    isObject: 'El campo data debe ser un objeto',
    isRequired: 'El campo data es requerido'
  }
}
